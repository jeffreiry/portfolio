# CLAUDE.md · Portfólio Jeferson Freiry

Instruções operacionais para o Claude Code trabalhar neste repositório.
Estas instruções têm prioridade sobre o comportamento padrão.

---

## O que é este projeto

Portfólio pessoal de **Jeferson Freiry — Product Designer Sênior**, voltado a
candidaturas para vagas sênior. Site próprio, em código, acessado via
`portfolio.jefersonfreiry.com`, com controle total de identidade, performance e conteúdo.

Objetivo de negócio: o portfólio é a peça central de recrutamento. Cada decisão
deve servir a **clareza da narrativa dos cases** e à **qualidade percebida**
(performance, tipografia, microinterações) — é a vitrine do trabalho de um designer.

---

## Stack

| Camada | Tecnologia | Por quê |
|--------|-----------|---------|
| Framework | **Astro 6** | Site centrado em conteúdo; zero-JS por padrão; content collections tipadas; i18n nativo |
| Linguagem | **TypeScript** | Frontmatter de cases tipado e seguro |
| Estilo | **Tailwind CSS v4** | Tokens do design system como utilitários; rápido de iterar |
| Interatividade | **Svelte 5 (ilhas)** | Aproveita o conhecimento do autor; só hidrata o que precisa |
| Conteúdo | **Content Collections (Markdown)** | Cases em Markdown puro, versionados no Git |
| i18n | **Astro i18n routing** | PT (padrão) + EN |
| Deploy | **Vercel** | Mesmo fluxo do projeto Painel Saúde |
| Analytics | **Vercel Web Analytics** + **Google Analytics 4** | Vercel nativo (sem cookies, `webAnalytics: true` no adapter); GA4 (`G-P3Y01ZJ3RT`) via `gtag.js` inline no `Base.astro`, adicionado 2026-08-20 — ID não é segredo, hardcoded direto no código |
| Fontes | **DM Serif Display** (display) + **Barlow 500/800** (corpo) | Ambas self-hosted em `public/fonts/` com subset latin para performance mobile |

> A stack foi decidida pelo Claude a pedido do autor. O racional completo e as
> alternativas consideradas estão em [docs/roadmap.md](docs/roadmap.md).

---

## Princípios inegociáveis

1. **Conteúdo antes de código.** Os cases ditam a estrutura, não o contrário.
2. **Markdown puro nos cases.** Sem componentes acoplados ao texto. Lacunas a
   preencher são marcadas com `⬜` (convenção do autor).
3. **Bilíngue de verdade.** Todo case e toda página de UI existem em PT e EN.
   Nunca deixar conteúdo só em um idioma sem marcar a lacuna com `⬜`.
4. **Zero-JS por padrão.** Só usar ilha Svelte quando houver interação real.
5. **Acessibilidade e performance não são fase final.** Lighthouse 95+ em todas
   as métricas é meta contínua, não um polish do fim.
6. **Identidade visual derivada do design system** em
   [docs/design-system.md](docs/design-system.md) — não inventar cores/tipos novos
   sem registrar lá primeiro.

---

## Documentação do projeto

| Arquivo | Para quê |
|---------|----------|
| [portfolio-readme.md](portfolio-readme.md) | Visão geral, estrutura de arquivos, como rodar |
| [docs/design-system.md](docs/design-system.md) | Cores, tipografia, espaçamento, componentes |
| [docs/cases-portfolio.md](docs/cases-portfolio.md) | **Guia-mestre dos cases**: régua sênior, template, narrativa, modelo técnico no Astro (arquivos, schema, i18n) + os cases já escritos (PT/EN) |
| [docs/home-copy.md](docs/home-copy.md) | Textos editáveis da home (hero, sobre, carreira, contato) em PT+EN — autor edita aqui, Claude aplica em `index.astro`/`pt/index.astro` |
| [docs/a11y-checklist.md](docs/a11y-checklist.md) | Checklist de leitor de tela (NVDA/VoiceOver) — passada manual de acessibilidade |
| [docs/roadmap.md](docs/roadmap.md) | Fases de construção, decisões técnicas e questões em aberto |
| [Bench_designers/_insights-melhorias.md](Bench_designers/_insights-melhorias.md) | Insights consolidados de benchmark de designers — referência para melhorias nas páginas de case |

Antes de implementar qualquer coisa, ler o doc relevante. Ao tomar uma decisão
de arquitetura nova, registrá-la na seção "Decisões técnicas" do roadmap.

---

## Convenções

- **Idioma da documentação e dos commits:** Português (PT-BR).
- **Idioma do código** (variáveis, componentes, rotas): Inglês.
- **Conteúdo dos cases:** PT e EN, em arquivos separados por idioma.
- **Commits:** mensagens curtas em PT, estilo `feat:`, `fix:`, `chore:`,
  `content:` (novo para entradas de case/conteúdo).
- **Nunca commitar** sem o autor pedir explicitamente.
- Marcar todo texto/dado ainda não definido com `⬜` em vez de inventar.
- **Todo deploy passa por revisão antes de considerar pronto** (regra desde 2026-08-19, depois do incidente de bypass de autenticação — ver histórico do CHANGELOG dessa data): build local, teste da mudança específica (localmente ou inspecionando o bundle compilado quando o comportamento depende de env vars/runtime), e conferência em produção depois do push — não só "buildou sem erro".

---

## Ao escrever ou reescrever cases

Antes de redigir ou revisar qualquer case, fazer as perguntas abaixo ao autor. São derivadas dos gaps transversais identificados em [Bench_job_applications/_index.md](Bench_job_applications/_index.md) — cada resposta preenchida aumenta o score de aderência em todas as vagas:

1. **Artefatos** — "Tem telas, flows ou wireframes disponíveis? (screenshots do Figma, Notion ou exportações)"
2. **Métricas** — "Há algum número de resultado real? (tempo economizado, usuários impactados, taxa de adoção, redução de erros)"
3. **Acessibilidade** — "Houve alguma decisão de acessibilidade no projeto? (contraste, daltonismo, leitor de tela, navegação por teclado)"
4. **Mobile** — "O produto tinha versão mobile ou responsiva? Houve decisões específicas de design para mobile?"
5. **Mentoria/liderança** — "Neste projeto, você fez onboarding de alguém, revisou o trabalho de outro designer, ou apresentou decisões de design internamente?"

---

## Decisões de produto fechadas

| Decisão | Escolha |
|---|---|
| Currículo | PDF linkado, versões PT e EN (`public/Jeferson_Freiry_PT.pdf` e `public/Jeferson_Freiry_EN.pdf`) |
| Contato | Formulário (envia para `jfreiry@gmail.com`) + botão WhatsApp |
| Modo escuro | Sim, com toggle claro/escuro |
| Microinterações | Expressivas — scroll reveals, hover com personalidade, transições de página |
| Visibilidade de case novo | `draft: true` só enquanto o projeto pessoal está incompleto (hoje: `cartela-cores`, `painel-saude`). Todo o resto fica `draft: false`. Cases sob NDA/empresa confidencial ganham `protected: true` (exige senha do `/login` ou `?access=` do link mágico — ver `PORTFOLIO_ACCESS_TOKEN`); cases com cliente nomeável ficam públicos sem `protected`. |

---

## Variáveis de ambiente necessárias

| Variável | Onde usar | Para quê |
|---|---|---|
| `PORTFOLIO_PASSWORD` | Vercel → Settings → Env Vars | Proteção por senha só dos cases com `protected: true` no frontmatter (não é mais `/work/*` inteiro) |
| `PORTFOLIO_ACCESS_TOKEN` | Vercel → Settings → Env Vars | Token de link mágico — `?access=TOKEN` em qualquer URL do site seta o cookie de sessão dos cases sem passar pelo `/login`. Separado da senha de propósito (dá pra trocar o link mandado a recrutadores sem mexer na senha manual). Gerar como string longa aleatória, nunca digitada — só existe embutida no link (ex: `openssl rand -hex 24`) |
| `JOBANALYSIS_PASSWORD` | Vercel → Settings → Env Vars | Senha do bench (`/jobanalysis`), totalmente independente dos cases (sem fallback pra `PORTFOLIO_PASSWORD`) |
| `RESEND_API_KEY` | Vercel → Settings → Env Vars | Envio de email do formulário de contato |
| `GROQ_API_KEY` | Vercel → Settings → Env Vars | Extração de estrutura da JD (passo 1 do pipeline em `/api/jobanalysis-analyze`), modelo `openai/gpt-oss-120b` |
| `ANTHROPIC_API_KEY` | Vercel → Settings → Env Vars | Análise de vagas com Claude (passo 2 do pipeline em `/api/jobanalysis-analyze`) |

O sender do Resend configurado é `contato@portfolio.jefersonfreiry.com` — o domínio precisa ser verificado no painel do Resend antes do envio funcionar em produção.

---

## Estado atual

**Fase 5 concluída. Fase 6 (fundação) implementada em 2026-06-19 — ver [docs/roadmap.md](docs/roadmap.md#fase-6).** Formulário de contato, dark mode e microinterações expressivas implementados. PDFs de currículo em `public/`. Acessibilidade: skip nav, foco visível, contraste AA auditado, segunda passada de código (2026-06-19). Sitemap implementado. Hero com vídeo `Gh011011.mp4` (autoral, 8MB) e carregamento condicional (mobile/slow-connection recebe fallback de imagem Unsplash). Revisão de voz concluída em 2026-06-11 (9 cases × 18 arquivos). Reestruturação de seções concluída em 2026-07-12: 7 cases publicados reduzidos de 11 → 6 seções (My Role + Constraints → Context & Problem; Technical Collaboration → Process & Decisions; Solution + Craft → Solution & Craft; What I'd Do Differently + Learnings → Learnings). Power Apps: 5 seções. Seção de voz humana (4.1) e template-mestre atualizados em docs/cases-portfolio.md. Lighthouse auditado em 2026-06-11: Desktop 99/95/100/100 ✅. Re-auditado mobile em 2026-06-19 após correções: FCP 1.0s (100), LCP 2.0s (97), Speed Index 1.0s (100) — meta de 95+ performance mobile atingida ✅. Bench de 55 vagas, média 57%; ver [Bench_job_applications/_index.md](Bench_job_applications/_index.md).

**Revisão de imagens e consistência visual-narrativa (2026-07-12):** Auditoria completa de todos os cases publicados. Regra implementada: imagens de solução não podem aparecer em seções de Contexto & Problema; alt text deve descrever elementos literalmente visíveis na tela. Correções aplicadas em 8 arquivos (EN+PT): Arezzo (01-store-view.png movida para Solution, alt text de 03-admin-view.png corrigido), Enterprise AI (02-user-research.png removida de Discovery, reposicionada em Solution), Del Valle Website (01-home.png removida de Context, reposicionada em Solution), Del Valle Kapo (01-smatphone.png removida de Context, reposicionada em Solution). Alt texts vazios `![]()` preenchidos: Enterprise AI (03-main-interface.png, 01-file-source.png EN+PT) e Shipping (01-route-calendar.png, 02-route-opportunities.png, 03-route-simulation.png EN+PT). Seção 4.2 "Imagens e texto — consistência visual-narrativa" adicionada em docs/cases-portfolio.md com 3 regras e checklist.

**Reposicionamento de imagens em todos os cases (2026-07-12):** Segunda passada: cada imagem aparece imediatamente após o texto que a descreve, sem imagens consecutivas sem texto entre elas. Moves em 12 arquivos (EN+PT): Arezzo (02-manager-view após D4, 03-admin-view após D3 — saiu do fim do Solution); Enterprise AI (03-main-interface após bullet 1, 02-user-research após bullet do source panel, 01-file-source após bullet de acessibilidade); Del Valle Website (02-tablet após D4, 03-smarphone após bullet de mobile); Del Valle Kapo (02-brincadeiras após D3, 03-brincadeiras-mobile após bullet da Hora de Brincar); Shipping (01-route-calendar após D1, 02 e 03 intercaladas com bullets no Solution); Power Apps (03-csat após lista de escopo, 03-faq após Brand Center, 02-actions após frase de replicabilidade). Título PT do Shipping corrigido. Next Case banner: usa `detectHero(nextCase.data.slug)` com fallback para `cover` e Unsplash (EN+PT em `work/[slug].astro`).

**Integração Painel Saúde (2026-06-15):** Widgets `RunWidget.astro` e `WorkoutWidget.astro` adicionados ao About, mostrando km mensais (Strava) e horas mensais de treino (Hevy) via API em `painel-saude-zeta.vercel.app`. Bottom row dos widgets em `flex-column` (count acima, "Syncing with" abaixo — evita quebra no mobile). Rebuild automático do portfólio via Vercel deploy hook sempre que o painel-saude detectar dados novos no sync diário (00:00 BRT). Logos em `public/logos/` (strava.png, hevy.png); brancos no dark mode via `filter: brightness(0) invert(1)`. Fundo dos widgets: `#fafafa` (light) / `var(--color-surface)` (dark) — igual aos cards de carreira. Widgets com scroll reveal (`data-animate`).

**Hero vídeo mobile (2026-06-19):** `Gh011011_mobile.mp4` (4.8MB, 640p) adicionado em `public/`. `Hero.astro` atualiza o JS para carregar a versão mobile em viewports < 768px e a versão desktop (8.1MB) em ≥ 768px. Conexão lenta ou `prefers-reduced-motion` mantém fallback de imagem. O `data-src-mobile` é derivado automaticamente do `bgVideo` via `.replace('.mp4', '_mobile.mp4')`.

**Dev local (2026-06-18):** `astro.config.mjs` exclui `**/.vercel/**` do Vite watcher para evitar `ELOOP: too many symbolic links` ao rodar `npm run dev`.

**Fix formulário de contato (2026-06-19):** `POST /api/contact` retornava HTTP 405 em produção porque, com `output: 'static'`, o Astro 6 prerenderiza todo endpoint por padrão e o `src/pages/api/contact.ts` não tinha `export const prerender = false` (diferente de `login.ts`/`logout.ts`). O endpoint virava arquivo estático em vez de função serverless. **Todo endpoint de API novo deve declarar `export const prerender = false`** enquanto o `output` for `static`. Também adicionado `console.error('[contact] Resend send failed:', error)` no caminho de falha para diagnóstico via logs da Vercel. Pré-requisitos do envio (que o build estático mascarava): `RESEND_API_KEY` setada na Vercel e domínio `portfolio.jefersonfreiry.com` verificado no Resend.

**Página /jobanalysis (2026-06-18, atualizada 2026-06-19):** Rota PT única (`src/pages/jobanalysis.astro`) com os resultados do bench de candidaturas: resumo, ranking de aderência, gaps transversais e metodologia. **Dados lidos dinamicamente via `fs.readdirSync`** dos arquivos `.md` em `Bench_job_applications/` a cada requisição SSR — não há array hardcoded de vagas. O parser extrai: score, empresa, cargo, produto, data, status, candidatura, blockquote de interpretação, tags de competências, gaps (bloqueadores/ausentes/boa aderência) e breakdown (obtObrig/maxObrigW/obtPref/maxPrefW). **Protegida por senha própria** (`JOBANALYSIS_PASSWORD`, independente dos cases): `prerender = false` + lógica de áreas em `src/auth.ts`. Defesa em profundidade: `noindex={true}` + fora do sitemap. **Nota (2026-08-19):** entre algum momento e essa data o middleware tinha ficado com um bloqueio incondicional (404 sempre em produção, sem checar senha nenhuma) — a página nunca esteve de fato acessível via senha, apesar da doc dizer o contrário. Corrigido: `/jobanalysis` agora usa o mesmo mecanismo de área/senha de `src/auth.ts` (`requireAuth` em `src/middleware.ts`), com `JOBANALYSIS_PASSWORD` configurada na Vercel. **Cards:** blockquote de interpretação, tags de competências, data de postagem, botão de candidatura (modal com toggle Sim/Não + status), filtros pill (Candidatura e Aderência), botão Editar (reabre modal de análise com slug pré-definido). **Expander "Ver análise":** seção inline colapsável por card — breakdown do score em duas mini-barras (Obrigatórios ×2 e Preferidos ×1 com pts obtido/máximo) + grid 3 colunas com 🔴 Bloqueadores, 🟡 Ausentes, 🟢 Boa aderência. Animação CSS via `grid-template-rows: 0fr → 1fr`. **Análise via IA — pipeline dois passos (atualizado 2026-08-19):** Passo 1: Groq `openai/gpt-oss-120b` extrai JSON estruturado da JD `{empresa, produto, cargo, nivel, data, obrigatorios[], preferenciais[]}` — só parsing, sem scoring. (Antes era `llama-3.3-70b-versatile`, descontinuado pela Groq — checar `GET /openai/v1/models` na Groq se esse modelo também sumir do catálogo no futuro.) Passo 2: Claude `claude-sonnet-4-6` recebe os requisitos estruturados + contexto completo do portfolio (9 cases com detalhes específicos, regras de evidência e gaps estratégicos) → gera análise completa com raciocínio estratégico contextualizado. **O servidor recalcula tudo deterministicamente:** score (math exato das tabelas), subtotais, tabela de cálculo. O Claude não faz aritmética — só avalia e raciocina. Requer `ANTHROPIC_API_KEY` no `.env` local e na Vercel. **Atenção técnica:** acessar `process.env` via variável local (`const _env = process.env`) para evitar que o Vite faça inlining estático das keys em build time — `process.env['VAR']` direto pode ser substituído por `undefined` no bundle. **Escrita de arquivo funciona apenas localmente** (filesystem read-only na Vercel) — na produção o card aparece na sessão mas não persiste ao recarregar. **Convenção (2026-08-19): toda vaga nova analisada passa por double-check do Claude antes de considerar pronta** — como a escrita só persiste local, o autor roda a análise em `npm run dev` (não em produção) e o Claude lê o `.md` gerado em `Bench_job_applications/` direto, conferindo score/gaps contra a JD real antes de dar como validado. A seção "Gaps transversais" é lida dinamicamente do `_index.md` (parser `parseGapsTransversais()`, mesmo arquivo lido pra listar as vagas) — não há mais array hardcoded pra sincronizar manualmente (fix 2026-09-12, depois de a versão hardcoded ficar desatualizada em produção sem ninguém notar). **Bench atual: 55 vagas, média 57%. "Atualizado em" na página é dinâmico (lê a data mais recente dos arquivos .md).** **Convenção (2026-08-20): quando uma vaga é reanalisada e colide com um slug existente, o pipeline gera um arquivo com sufixo de timestamp em vez de sobrescrever** — nesses casos (e em duplicatas de submissão), manter sempre a versão mais recente (conteúdo + eventual status de Candidatura atualizado) sob o nome de slug limpo, e apagar o arquivo com timestamp. **Convenção (2026-09-08): vagas com `Status: Recusado` ganham um campo opcional `**Motivo da recusa:**`** no arquivo, logo abaixo de `**Status:**` — 1-2 frases sintetizando a causa provável da recusa a partir dos 🔴 Bloqueadores já presentes na análise, distinguindo recusa por gap real de portfolio (score baixo coerente) de recusa por filtro binário eliminatório (idioma, plataforma, etc. — geralmente com score alto, já que não é sobre qualidade do trabalho). **Não é gerado automaticamente por IA na troca de status** (decisão do autor: prefere pedir a análise explicitamente) — ao autor pedir para "atualizar o bench", checar vagas com `Status: Recusado` sem esse campo preenchido e produzir a análise antes de considerar a atualização completa. Exibido na `/jobanalysis` como card com borda e tag vermelhas (`#b91c1c`, automático via `data-status="Recusado"` em CSS — nenhum código novo necessário por vaga) + um callout com o motivo, quando preenchido. **Nota de especificidade CSS:** a regra `.vaga-card[data-status="Recusado"]` precisa ser prefixada com `section` (`section .vaga-card[data-status="Recusado"]`) pra empatar em especificidade com a regra base `section .rounded-2xl.border` — sem isso a borda cinza padrão vence mesmo com o data-attribute certo. **Convenção (2026-09-13): seção "Matriz FOFA"** (`## Matriz FOFA` no `_index.md`, 4 sub-tabelas `### 🟩 Forças / 🟥 Fraquezas / 🟦 Oportunidades / ⬛ Ameaças`) — lida dinamicamente pela página via `parseFOFA()` (mesmo princípio de fonte única dos Gaps transversais), mas com cadência de atualização diferente: **não é recalculada a cada vaga nova, só quando o autor pedir explicitamente** ("atualizar a FOFA"). Ao implementar essa seção, corrigidos dois bugs: (1) `parseGapsTransversais()` não tinha limite superior — lia até o fim do arquivo, então a Matriz FOFA (adicionada logo depois no `_index.md`) teria suas tabelas engolidas pra dentro dos Gaps transversais também, já que ambas começam com `|`; fix: `.split(/\n## /)[0]` limita a seção ao próximo heading de nível 2. (2) **A faixa "Alta aderência" (≥80%) nunca existia na lógica de `banda()`/filtro/cor** — só na legenda "Interpretação do score", que documentava 4 faixas enquanto o código só implementava 3 (parcial/baixa/muito-baixa), fazendo um score de 95% cair no mesmo bucket visual de um 61%. Corrigido nos 2 lugares duplicados (script do servidor + `<script>` client-side) — `Banda` agora tem 4 valores, com pill de filtro "Alta" e cor própria (`--color-tag-research`).

**Currículos PT/EN reescritos (2026-09-08/09):** `public/Jeferson_Freiry_{PT,EN}.pdf` não têm arquivo-fonte (Word/Canva/etc.) — o PDF é a única versão, editado diretamente via `pdf-lib` + `@pdf-lib/fontkit` (texto removido de verdade do content stream, nunca só coberto com retângulo, pra não vazar em cópia de texto/parsing de ATS). Fonte original do documento é Liberation Sans (clone livre da Arial) com encoding customizado por subset — edições novas usam Arial real do Windows (`C:/Windows/Fonts/arial.ttf` + itálica + bold), visualmente equivalente mas não pixel-idêntica. PT e EN **não são geometricamente idênticos** (o texto em inglês quebra linha diferente, deslocando seções inteiras para Y's distintos) — qualquer reflow de layout precisa ser recalculado a partir da estrutura real de cada arquivo, nunca copiado de um pro outro. Conteúdo atual: título sem "Sênior/Senior" (evita filtro automático de ATS em vagas Pleno), LinkedIn no cabeçalho, Resumo em bullets com métricas reais, sem menção a mobile (não sustentado por case nenhum), Experiência com empresa+local+datas numa linha e cargo em itálico na linha de baixo. **Fluxo de edição de conteúdo (`curriculo-pt-draft.md`, raiz do projeto, não versionado):** como o PDF não tem arquivo-fonte editável, o autor edita o texto num `.md` de trabalho (não é doc do projeto, é rascunho — convenções: `**negrito**` = empresa, `*itálico*` = cargo) e devolve pro Claude aplicar de volta no PDF via `pdf-lib`, preservando layout/fonte/posicionamento atuais. O autor só se preocupa com o conteúdo; formatação fica por conta de quem aplica.

**Variante identificada (2026-09-07): quando a JD colada falha/vem incompleta, o pipeline pode gerar um slug DIFERENTE (não colide) com produto/área "Não especificado", JD não arquivada (`⬜`) e score artificialmente alto/perfeito** — sinal de dado não confiável, não de vaga real com alta aderência. Caso real: `zuri-product-designer-senior.md` (95%, sem JD, criado 18min antes da reanálise correta) vs. `zuri-product-designer-senior-experiencia-em-investimentos.md` (56%, JD completa, candidatura real). Ao encontrar um par empresa+data com um dos dois sem JD arquivada, tratar como o mesmo caso do double-check: a análise sem JD é inválida e deve ser apagada, não contabilizada no bench. **Fix (2026-08-20): `max_tokens` do passo 2 (Claude) subiu de 4000 para 8000** — vagas com JD muito longa (muitos requisitos + competências comportamentais, ex: Renner) geravam análise longa o suficiente pra estourar o limite antigo, truncando a resposta antes do marcador `---METADATA---` e quebrando o parse (`raw.split('---METADATA---')` retornava `undefined`) — surgia pro usuário como "A IA retornou um formato inesperado." Adicionado log de `stop_reason === 'max_tokens'` pra diagnosticar mais rápido se acontecer de novo com JDs ainda maiores.

**Seção About (2026-06-15):** Education/Formação e label de hobbies usam estilo de label 11px uppercase (`font-body`, 600, 0.12em letter-spacing, `color-faint`). Botão de currículo: "Download my Resume" / "Baixe meu Currículo". Logo header: `jeferson-black.png` (light) / `jeferson-white.png` (dark).

**Seção Cases (2026-06-15):** Tags reestruturadas para 7 categorias de filtro: Enterprise, AI & Automation, UX Research, Design System, Branding, Mobile, Data & Analytics. Drafts recebem tag oculta "Projetos Pessoais". Filtro interativo (JS vanilla, `<script>` inline) adicionado acima do grid em EN e PT — botões pill, "All/Todos" ativo por padrão. `CaseCard.astro` simplificado: campo `role` removido do card, exibe apenas o `year`.

**Acessibilidade WCAG AA (2026-06-15):** Auditoria de contraste por script (`f(c)` luminância relativa). Tokens novos: `--color-jambu-text` (#a8480c light / jambu dark) para jambu como texto/borda; `--color-jambu-deep` (#a8480c) para texto branco sobre laranja (Hero CTA, submit, skip-link). `--color-faint` escurecido em ambos os modos para ≥4.5:1. Bars decorativas mantêm `--color-jambu`. Formulário de contato: validação acessível (`aria-invalid`/`aria-describedby`/erros inline), foco gerenciado, `aria-live`. **Ao criar elementos novos: nunca usar `--color-jambu` como texto sobre fundo claro nem texto branco sobre `--color-jambu` — usar os tokens `-text`/`-deep`.** Pendente: passada de leitor de tela end-to-end.

**A11y e performance (2026-06-19):** Segunda passada de código de acessibilidade: `login.astro` Google Fonts → self-hosted (eliminava render-blocking); `role="alert"` + `aria-required` + `aria-describedby` no login; filter buttons com `aria-pressed` + live region de resultado; `CaseCard` `h2` → `h3` (hierarquia correta); "Other activities" `<p>` → `<h3>`; `ThemeToggle` com `focus-visible`; dots pulsantes dos widgets com `prefers-reduced-motion`; `ContactForm` `client:load` → `client:visible` (reduz TBT). Performance: `preconnect` + `dns-prefetch` para `images.unsplash.com` (hero LCP). Gargalo restante: hero `bgImage` externo — mover para asset self-hosted fecha o Lighthouse mobile.

**Sitemap (2026-06-19, atualizado 2026-08-19):** `astro.config.mjs` com `customPages` — hoje só os 4 cases públicos (EN + PT = 8 URLs); os 3 protegidos por senha/link mágico saíram da lista quando viraram `protected: true`. Filtro para `/jobanalysis` e `/login`. `public/robots.txt` criado.

**Convenção (2026-09-13): posicionamento variável dos cards de métricas.** `entry.rendered.html` (API do content layer do Astro, populada automaticamente pra qualquer entrada markdown) expõe o HTML já compilado como string — sem precisar reprocessar o pipeline de markdown. Um marcador `<!--metrics-here-->` no corpo do `.md` de um case diz onde o grid de métricas (`MetricsGrid.astro`, componente novo e reutilizável) entra dentro do texto; `htmlBefore`/`htmlAfter` vêm de `renderedHtml.split(METRICS_MARKER)`, renderizados via `<Fragment set:html={...} />` dos dois lados. **Sem marcador no arquivo, cai no comportamento antigo** (grid no topo da página, antes do `<article>`) — nenhum case quebra por falta do marcador. Posição escolhida caso a caso (após Discovery & Insight, após Context & Problem, antes de Outcome, etc.) — de propósito, pra não repetir sempre o mesmo lugar em todo case. Aplicado nos 9 cases (PT+EN); os 4 que não tinham `metrics` no frontmatter (Arezzo, HYPERGESTOR, Del Valle Website, Del Valle Kapo) ganharam números **já citados no próprio texto do case** (nunca inventados — ex: "130+ atividades" e "5 categorias" do Kapo, "3 perfis" do Arezzo, "4 módulos" do HYPERGESTOR). Grid também deixou de ser exclusivo do `template: 'enterprise'` — agora aparece em qualquer template quando há `metrics`. **Correção junto:** o grid antigo usava `grid-cols-4`, que com menos de 4 métricas deixava o grupo colado à esquerda em vez de centralizado; `MetricsGrid.astro` usa `flex flex-wrap justify-center`, sempre centralizado como grupo independente da contagem. `cartela-cores` e `painel-saude` já receberam o marcador e as métricas continuam corretas no frontmatter, mas como ambos são `draft: true`, a mudança fica inerte até serem publicados.

**Capa do LinkedIn (2026-09-13, fora do repo — entregável, não asset do site):** gerada via SVG + `sharp` (mesma técnica dos diagramas de `jobanalysis-case-study`), 1584×396 (tamanho oficial de capa). Usa cores reais do design system (`--color-cha-mate-light` #7a3f1e → `--color-page-bg` dark #18120e em gradiente, `--color-jambu` #ed6707 no destaque) e o logo `public/jeferson-white.png` (mesmo bracket wordmark do `og-default.png`, versão branca). **Fontes customizadas (DM Serif Display/Barlow) não embutem no SVG via `sharp`** — testado com `@font-face` + base64 woff2, cai silenciosamente pro fallback do sistema; solução foi usar Arial bold tracked (uppercase, letter-spacing), que por acaso já bate com o estilo "eyebrow" do site. **Zona de segurança real (descoberta ao aplicar e revisar print real):** a foto de perfil do LinkedIn é bem maior que o esperado (~280px de diâmetro, não ~200px) e cobre boa parte do canto inferior esquerdo (aprox. x:0-340, y:140-396 num canvas de 1584×396) — qualquer elemento decorativo nessa área fica escondido; o próprio ícone de edição do LinkedIn (só visível ao dono) também ocupa o canto superior direito, perto de onde ficaria um logo colado no topo. Arquivo final fica em `C:\Users\Pichau\Downloads\linkedin-capa-jeferson-freiry.png` — não faz parte do repo/deploy, é entregável avulso.

**Fix (2026-09-13): `public/og-default.png` não existia.** `Base.astro` sempre teve `ogImage = '/og-default.png'` como fallback (`og:image`/`twitter:image`, 1200×630) — mas nenhuma página do site jamais passou um `ogImage` próprio, então todo o site inteiro (home EN/PT, os 9 cases, contato, login) dependia exclusivamente desse fallback, que nunca foi adicionado em `public/`. Na prática, qualquer link do portfólio compartilhado em LinkedIn/WhatsApp/X mostrava preview quebrado ou sem imagem, provavelmente desde o lançamento. Corrigido adicionando o arquivo (logotipo "JEFFERSON" em bracket, fundo branco, 1200×630 exato).

**Responsividade fina e a11y de contraste (2026-06-23):** Education e Activities grids na home (EN+PT) corrigidos de `grid-cols-2` fixo para `sm:grid-cols-2` — em mobile (<640px) ficam em 1 coluna. Tables nos cases receberam `display: block; overflow-x: auto` para evitar vazamento em viewports estreitos. `CaseCard` tags: `--text-secondary` → `--text-primary` sobre `--bg-surface-alt` (contraste era ~3.5:1, agora ≥7:1, WCAG AA cumprido).

**Fase 6 — Design System maturity (100% concluída, 2026-06-19):** Tokens de tipografia (`--text-*`, `--leading-*`, `--tracking-*`), motion (`--duration-*`, `--ease-out-expo`), raios e espaçamento adicionados ao `@theme`. Tokens `--text-section` (28px) e `--text-card` (24px) adicionados para fechar gaps da escala real. Camada semântica em `:root` + utilities CSS (`text-heading`, `text-primary`, `text-secondary`, `text-subtle`, `bg-surface-alt`). Migração total: `CaseCard`, `Header`, `Footer`, `RunWidget`, `WorkoutWidget`, `ContactForm`, `index.astro` EN+PT, `work/[slug].astro` EN+PT — zero primitivos no markup público. Tamanhos px arbitrários migrados para tokens (`text-[28px]` → `text-section`, `text-[10px]` → `text-caption`, etc.). `ContactForm` migrado para tokens semânticos + `var(--radius-input/pill)`. `design-system.md` completo: escala tipográfica, matriz de 5 variantes de botão, 4 estados de UI (empty/loading/error/success), iconografia formalizada. **Regra:** todo componente novo usa apenas tokens semânticos — nunca primitivos diretamente.

**Imagens dos cases (2026-06-20):** Imagens reais adicionadas em `public/cases/{slug}/`. Convenção de arquivos por case: `card-home.*` (imagem da home card) + `hero.*` (background do hero do case) + arquivos numerados `01-*.png`, `02-*.png`, `03-*.png` para screenshots inline. `src/utils/cover.ts` — `detectHero(slug)` procura `hero.*` → fallback `visao-geral.*`; `detectCardImage(slug)` procura `card-home.*` → fallback `visao-geral.*`; ambas via `fs.existsSync`. Usado em `index.astro` (EN+PT) como `cover={detectCardImage(slug) ?? c.data.cover}` e em `work/[slug].astro` (EN+PT) como `heroBg = detectHero(slug!) ?? entry.data.cover ?? bgImage`. Cases sem `card-home` ou `hero` fazem fallback para `visao-geral.*` ou `bgImage` Unsplash — nenhum case quebra. Placeholders `<div class="image-placeholder">` substituídos por `![alt](path)` nos cases com screenshots disponíveis (PT+EN). Extensões `.jpg` corrigidas para `.png` nos frontmatters de arezzo, del-valle-kapo, del-valle-website e power-apps-dummy-app.

**Redesign do case Del Valle Kapo + novo template `brand-split` (2026-09-14):** Sessão iterativa de redesign visual e de conteúdo do case, guiada por referência real (Lawrence Zheng/ICPSR) e por materiais de campanha reais fornecidos pelo autor. **Novo template `template: brand-split`** (`src/components/HeroBrandSplit.astro`, schema em `content.config.ts`, wiring em `work/[slug].astro` EN+PT) — inicialmente só no Kapo, depois também aplicado no Del Valle Website (2026-09-14, ver entrada mais recente); Arezzo continua em `template: brand`: hero em duas zonas (título/tese/tags sobre o creme da página, depois uma faixa branca com a imagem de capa contida em 16:9 subindo por `margin-top` negativo pra sobrepor a costura entre as duas zonas — `display: flow-root` na faixa branca evita que a margem negativa colapse e arraste a própria caixa junto). Campos novos opcionais `scope: string[]` e `duration: string` no schema — quando presentes, o hero troca Atuação/Empresa/Ano por Atuação/Escopo(lista)/Duração. Coluna do `<article>` em 1160px (mais larga que os 720px padrão), mas imagens comuns do corpo ficam contidas em 900px centralizadas via `.case-content--narrow-images` — só imagens dentro de `.before-after` preenchem a largura toda; classe `.case-content--no-manifesto` evita que o primeiro H2 do corpo (agora sem tese duplicada, já que a tese vive só no hero) receba por engano o estilo de citação-manifesto pensado pro `h2:first-of-type` dos outros templates. **Lightbox genérico:** qualquer imagem dentro de `.case-content` (todos os cases, não só Kapo) agora é clicável — abre em overlay fullscreen, fecha com clique ou Esc (`initLightbox()` em `work/[slug].astro` EN+PT). **Conteúdo reescrito com dados reais** trazidos pelo autor ao longo da sessão: 2 personas com fichas demográficas reais (`del-valle-kapo-personas-{pt,en}.png`), sitemap real da IA, benchmark competitivo **ilustrativo** (a imagem e o texto deixam isso explícito — não é pesquisa de mercado formal), 3 comparações antes/depois reais (site principal, microsite da campanha "Sempre Criança Challenge", categoria "Ritmo de Brincadeira"), 2 seletores lado a lado (produtos na home vs. atividades no microsite — mesma linguagem de seleção de personagem de videogame reaproveitada em dois contextos). Métricas do frontmatter trocadas para 4 números reais de campanha (117 dias, 400 mil PDV, 60 brincadeiras mapeadas, 12 ilustrações), à parte trocadas por instrução explícita do autor: **toda menção a Google Analytics/"dados" foi removida do case** (a alegação original de arquitetura "data-driven" não era sustentável — reescrito em cima de personas + benchmark + comportamento observado, sem citar fonte de analytics nenhuma). 3 imagens de lifestyle/mockup antigas (`01-smatphone.png`, `02-brincadeiras.png`, `03-brincadeiras-mobile.png`) removidas do case — mostravam as mesmas telas que os novos screenshots reais, mas em fotos de contexto (celular na mão, crianças no fundo) em vez de screen puro; arquivos continuam em disco, só não referenciados. **Pendência (2026-09-14): mobile não está documentado/ilustrado no case** apesar de ser tag do card — autor confirmou que vai trazer isso numa sessão futura.

**Redesign do case Del Valle Website + correções sitewide (2026-09-14):** Mesma base do Kapo aplicada aqui — `template: brand-split`, hero em duas zonas, coluna de 1160px, assets reais (sitemap, wireframes responsivos, antes/depois da Home, screenshot da página "Fruta toda hora", 4 cards do carrossel de frutas). **Novo campo de schema `bodyManifesto` (default `true`)** — o Kapo usa `bodyManifesto: false` (tese só no hero, sem duplicar no corpo); o Del Valle Website usa o default `true` porque mantém tese do hero e tese do corpo como textos distintos (convenção normal do site) — antes disso, `case-content--no-manifesto` estava amarrado incondicionalmente a `template === 'brand-split'`, o que teria quebrado a estilização do H2 aqui. **Nova classe `.contrast-box`** (fundo `--color-surface` + padding) — resolve cards com metade branca (como os do carrossel de frutas) ficando "branco sobre branco" na zona de conteúdo. **Mockup de 3 celulares** (`mobile-scroll-3phones.png`) montado com `sharp` a partir de captura real de tela cheia, recortado pra nunca cortar uma foto no meio (mesma disciplina de crop aplicada nos diagramas). **Duas correções de fundo por engano de sessões anteriores, ambas confirmadas pelo autor e corrigidas:** (1) o texto tinha o conteúdo de "Home" e "Fruta toda hora" invertido em relação aos screenshots reais; (2) o role do case dizia "Visual Designer" e atribuía criação de ilustrações/elementos gráficos ao autor — na real ele atuou só como UX/UI Designer (identidade veio de outra equipe), `scope`/`duration` também corrigidos (1 mês, não 2). **Seção Processo & Decisões inteiramente reescrita** — as 4 decisões anteriores eram fabricadas; mantida só a decisão confirmada como real (carrossel de 4 dimensões), com honestidade explícita sobre o que não foi decisão do autor (arquitetura das 3 páginas e escolha de hero por página vieram do cliente/agência), mais uma reflexão sobre como um rebranding "aparentemente reskin" é oportunidade pra um designer sênior aplicar diretrizes de usabilidade em pontos pequenos (ex: link da Loja como botão com contorno + ícone de carrinho, sinalizando saída do site). **Convenção sitewide: `**Resumo**` substituiu `**TL;DR**` em todos os 8 cases PT restantes** (EN mantém `TL;DR`). **Badge "Under Construction" removido do Kapo e do Del Valle Website** (considerados prontos); Arezzo e HYPERGESTOR continuam marcados.

**Estratégia de busca ampliada (desde 2026-09-07):** empregador atual do autor entrou em recuperação judicial (90 dias de prazo a partir de 2026-09-07) — risco real de desemprego. Busca ampliada para incluir vagas **Pleno no exterior** (salário em USD pode superar o senior BR atual) em paralelo às vagas Senior (BR e exterior), priorizando velocidade de resposta. **Decisão: manter os mesmos cases do portfolio para os dois públicos** — não criar segunda versão; o ajuste de nível acontece só na cover letter/discurso por vaga, nunca no conteúdo publicado.

**Próximas ações por criticidade:** 🔴 métricas de impacto (`⬜`), 🔴 mobile nativo (bloqueador em 13 vagas do bench, nenhum case cobre), 🟠 acessibilidade enterprise nos cases, 🟠 passada manual de leitor de tela (NVDA/VoiceOver).

Estrutura de código real: `src/components/`, `src/layouts/`, `src/pages/`,
`src/content/cases/{pt,en}/` (9 cases × 2 idiomas — 4 públicos + 3 protegidos por senha + 2 draft), `src/middleware.ts`
(proteção por senha por case individual via campo `protected` no frontmatter — não é mais o `/work/*` inteiro, ver `src/auth.ts`), `src/pages/api/login.ts` + `logout.ts`.

---

## Backlog — Job Analysis

Levantado em 2026-09-13, consolidando itens que já estavam espalhados pelo `CHANGELOG.md` mais achados da própria sessão de revisão. Não é recalculado automaticamente — atualizar manualmente quando um item for resolvido ou um novo for identificado.

**🔴 Bugs/riscos conhecidos, sem correção definitiva:**
- `JOBANALYSIS_PASSWORD` não é lida via `process.env` no dev server local (identificado 2× nesta sessão) — impede login local pra validar mudanças na página visualmente.
- Detecção de "score suspeito sem JD arquivada" (padrão do caso Zuri, 2026-09-07) é regra manual — só existe porque o autor lê os arquivos; nada no pipeline sinaliza automaticamente.
- Colisão de slug em reanálise exige reconciliação manual (apagar arquivo com timestamp) — já resultou em 20+3 vagas órfãs esquecidas em duas ocasiões (2026-09-07 e 2026-09-12).
- `max_tokens` do Claude já estourou uma vez (Renner, JD longa) — o fix (4000→8000 + log de `stop_reason`) é reativo, não preventivo; uma JD ainda maior pode estourar de novo.
- Modelo da Groq já foi descontinuado uma vez sem aviso (`llama-3.3-70b-versatile`) — nenhum monitoramento ativo, só descoberto quando quebrou em produção.

**Convenção (2026-09-13): double-check recorrente disparado por mudança no portfólio, não por calendário.** O double-check original (registrado em 2026-08-19) só cobre a vaga no momento da criação — cobre o risco de o Claude alucinar ou truncar *naquela* análise. Não cobre o risco maior: a JD é fixa (já aconteceu), mas o portfólio muda (nova métrica, novo artefato, novo case publicado) — e uma vaga validada há meses não sabe disso, ficando com score/gaps desatualizados sem ninguém notar. Regra: toda vez que uma edição em `src/content/cases/` mexer num dos Gaps transversais (preencher `⬜` de métrica, publicar artefato visual, cobrir mobile nativo, etc.), perguntar "quais vagas do bench perderam ponto justamente nesse gap?" e reavaliar só essas — nunca reler o bench inteiro por rotina, isso não escala e ninguém sustenta.

**🟡 Gaps de produto:**
- Não existe auditoria sistemática da consistência do Claude entre as vagas do bench — é a premissa mais arriscada do próprio case "Job Analysis" e hoje só é checada por leitura manual.
- Não rastreia conversão candidatura → entrevista — não há dado pra saber se o "Motivo provável" muda comportamento real ou se o score prediz resultado.
- "Matriz FOFA" (`_index.md`) é snapshot manual — mesma classe de risco que os Gaps transversais tinham antes do fix de 2026-09-12 (fica obsoleta silenciosamente se ninguém pedir atualização).
- `fofaCor` em `jobanalysis.astro` mapeia cor por emoji exato do quadrante (`🟩`/`🟥`/`🟦`/`⬛`) — se o emoji de um quadrante mudar no `_index.md`, a cor cai no fallback cinza sem erro nenhum.

**🟢 Pendências de conteúdo:**
- Screenshots reais do `/jobanalysis` para o case `jobanalysis-case-study` (marcados `⬜` no `.md`, PT+EN).

**Convenção (2026-09-13): gestão de pipeline (Prioridade/Funil/Board/prazos).** Campo `**Status atualizado em:**` carimbado deterministicamente pelo servidor (`jobanalysis-analyze.ts` na criação/reanálise, `jobanalysis-update.ts` na edição) — nunca escrito à mão. Alimenta o badge de "dias parado" (≥21 dias em estágio ativo) e a seção "Prioridade agora" (top 5 por score desc + tempo parado desc). Campo opcional `**Prazo de candidatura:**` alimenta badge de prazo (vermelho se ≤3 dias). "Funil de candidaturas" e "Board por status" agrupam por `funilAtivo = ['A avaliar', 'Candidatura enviada', 'Entrevista agendada', 'Em processo', 'Proposta recebida']` — Recusado/Arquivado são estados terminais, contados à parte. Checklist de preparo pré-entrevista (bloqueadores viram checkbox) é client-side, não persiste. Aviso de empresa duplicada compara `data-empresa` do card novo contra os existentes na criação (não na reanálise). **Deliberadamente fora de escopo:** superfície automática da convenção de double-check recorrente (associar "gap fechado no portfólio" → "vagas que perderam ponto nesse gap" exigiria matching semântico texto-livre, frágil demais pra confiar sem validação melhor) — continua sendo checagem manual.

**Convenção (2026-09-13): `**Candidatura enviada em:**` é imutável, diferente de `**Status atualizado em:**` (que é sobrescrito a cada mudança).** Carimbado uma única vez em `jobanalysis-update.ts`, só na primeira vez que o status vira "Candidatura enviada" — nunca mais tocado depois, mesmo que a vaga avance pra Entrevista/Recusado/etc. É o que permite calcular `diasAteResposta` (badge "respondeu em N dias", verde) quando a vaga sai de "Candidatura enviada" pra qualquer estágio seguinte: `Status atualizado em` (do estágio atual) menos `Candidatura enviada em`. Sem os dois campos separados, a data de envio se perderia assim que o status avançasse de novo.

**Convenção (2026-09-13): indicador de score mudado.** Quando uma reanálise muda o score de uma vaga já validada, o arquivo ganha dois campos opcionais logo abaixo de `**Candidatura:**`: `**Score anterior:** X% (data da análise original)` e `**Motivo da mudança de score:**` (1-3 frases explicando *o que* mudou — não é auto-gerado, é escrito comparando as duas análises, idealmente puxando o diff real do git quando o histórico existir). A página renderiza um badge (↑ verde / ↓ vermelho) com o "de X% para Y%" + o texto do motivo logo abaixo, nos dois lugares onde os cards são montados (SSR inicial e o template client-side usado após editar/reanalisar). **Exemplo real implementado:** ADP (89%→76%, 2026-08-20) — os pontos obtidos não mudaram (32/32), o que mudou foi a granularidade da extração da JD: "Leadership" (mentoria de juniores) virou requisito próprio com nota 1 na 2ª passada, em vez de ficar absorvido num requisito genérico com nota 3 na 1ª — aumentando o denominador de 18 para 21. Reconstruído comparando as duas versões do arquivo via `git show <commit>:Bench_job_applications/adp-senior-ux-designer.md` (o histórico git preserva o "antes", mesmo quando o arquivo em disco já foi sobrescrito pela reanálise).
